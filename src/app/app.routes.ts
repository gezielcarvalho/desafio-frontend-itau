import { Routes } from '@angular/router';
import { BusinessListComponent } from './business/business-list/business-list.component';

export const routes: Routes = [{ path: '', component: BusinessListComponent }];
